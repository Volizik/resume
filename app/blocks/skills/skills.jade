mixin skills(data)
	+b.skills&attributes(attributes)
		+e(ul).scale
			each val in [5, 6, 7, 8, 9, 10]
				li= val
		each elem in data
			+e.row
				+e('span').item #{elem.name}
				+e.progress
					+e.progress--status(style='width: #{elem.value}')